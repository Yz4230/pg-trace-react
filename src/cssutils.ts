const url = (url: string) => {
  return `url('${url}')`;
};

const ROBOTO_FONT_FAMILY = "Roboto, sans-serif";
const SANS_FONT_FAMILY = '"Noto Sans JP", sans-serif';
const SERIF_FONT_FAMILY = '"Noto Serif JP", serif';

export { url, ROBOTO_FONT_FAMILY, SANS_FONT_FAMILY, SERIF_FONT_FAMILY };
